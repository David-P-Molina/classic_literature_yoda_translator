<% if session[:user_id] %>
  <h1>Welcome <%= @user.username %>! You are now logged in!</h1>
<h4>Lets get to translating</h4>
<% else %>
    <%= render partial: 'users/form', locals: {user: @user} %>
  <%= link_to('Log in with Facebook!', '/auth/facebook') %>
<% end %> 